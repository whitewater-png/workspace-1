# プロジェクト概要

Discord bot から LM Studio でローカルホストしている LLM（Gemma2 など）を呼び出す連携ツール。
- `main.py` — Discord bot 本体（`!ask`, `!code`, `!summarize`, `!translate`, `!help_claude` コマンド）
- `lm_studio_handler.py` — LM Studio の OpenAI互換API を呼び出すハンドラー
- `.env.example` — `DISCORD_TOKEN` / `LM_STUDIO_URL` の設定テンプレート

## 開発ワークフロー（計画 / 実装 / セキュリティレビューの役割分担）

このリポジトリでは変更を3つの役割に分けて進める。それぞれ専用の subagent を `.claude/agents/` に定義している。

1. **計画（`planner` エージェント）** — 何を・どこに・どう変更するかを決める。読み取り専用で、コードは書かない。
2. **実装（`coder` エージェント）** — 承認された計画に従ってコードを書く。既存のスタイル（async/await、`discord.py` コマンド、日本語のユーザー向けエラーメッセージ）を踏襲し、最小限の差分にする。
3. **セキュリティレビュー（`security-reviewer` エージェント）** — コミットやPR作成の前に、変更差分をセキュリティ観点（トークン/シークレットの扱い、コマンドインジェクション、未検証の外部入力、依存パッケージなど）でチェックする。

非自明な変更（新機能の追加や既存コマンドの挙動変更など）は、この3段階（planner → coder → security-reviewer）を順番に通すこと。typoの修正など自明に安全な変更は省略してよい。

## その他の方針

- コメントは非自明な「なぜ」を説明する場合のみ付ける。
- 推測での抽象化・将来を見越した過剰設計はしない。要求された範囲だけを実装する。
